<?php
/**
 * No servidor: copie para backend.local.php (mesma pasta que proxy.php).
 * Não commite backend.local.php no Git.
 *
 * API Go AD-Hub — porta predefinida 3041 (mesmo `PORT` no .env ao lado do binário).
 * Só altere para 3042 (ou outra) se tiver de fugir a conflito no servidor; nesse caso
 * use o mesmo URL que o processo Go está a escutar.
 */
return 'http://127.0.0.1:3041';
