<?php
/**
 * No servidor: copie para backend.local.php (mesma pasta que proxy.php).
 * Não commite backend.local.php no Git.
 *
 * Só necessário se o PHP não alcançar 127.0.0.1:PORT (ex. CageFS). A porta deve coincidir com
 * `PORT` no .env do binário — nunca mantenha um URL obsoleto (ex. http://127.0.0.1:3044).
 */
return 'http://127.0.0.1:3041';
