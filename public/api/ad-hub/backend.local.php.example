<?php
/**
 * No servidor: copie para backend.local.php (mesma pasta que proxy.php).
 * Não commite backend.local.php no Git.
 *
 * Use quando a porta 3041 ficar "presa" no jailshell (ss mostra LISTEN mas
 * lsof/fuser não mostram PID) — suba a API noutra porta no .env (ex.: PORT=3042)
 * e aponte aqui o mesmo URL.
 */
return 'http://127.0.0.1:3042';
